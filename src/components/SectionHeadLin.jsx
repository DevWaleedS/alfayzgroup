import React from "react";

const SectionHeadLin = ({ children }) => {
	return (
		<h2 className=' headline-2-ar !font-bold text-right text-grayscale-600 '>
			{children}
		</h2>
	);
};

export default SectionHeadLin;
